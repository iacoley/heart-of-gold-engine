aiohttp==3.13.5
aiosqlite==0.22.1
discord.py==2.7.1
numpy==1.26.4
fastembed==0.3.6
schedule==1.2.2
prompt-toolkit==3.0.48
rich==13.9.4
PyYAML==6.0.2
# Pinned to a tag, not a floating branch, deliberately — see
# bin/speaking_banana.py's module docstring. Cavern-wide adoption
# 2026-08-29 (Ian approved, task-1788046725): Amos pinned lib/
# speaking_banana.py to the same ref for cross-bot alignment; bump only
# as a deliberate, reviewed step, not silently via an unpinned "latest".
#
# 2026-08-30: bumped 9298d7d -> v0.1.1 (commit cbc900d). The prior pin
# had a real clean-install bug (setuptools flat-layout auto-discovery
# saw two top-level dirs, banana/ and specs/, and refused to build —
# reproduced independently with `pip install git+...@9298d7d` in a
# throwaway venv, not just Zero's report). This host's .venv had been
# running on an undocumented local pyproject.toml patch applied to a
# manual /tmp checkout, which masked the bug here while leaving any
# fresh install elsewhere broken. v0.1.1 carries Zero's upstream fix
# ([tool.setuptools.packages.find] include = ["banana*"]) plus the
# mandatory-`holder`-arg fix; verified with a clean `pip install` in a
# fresh venv before landing this bump. See #agent-chat 2026-08-29/30 for
# the coordination thread with Zero/Ryan/Amos.
#
# 2026-09-01: bumped v0.1.1 -> v0.2.0 (commit abaaf9f1). Adds the
# round-governor primitives (round field on the envelope, should_reply()
# soft-clamp at round 2, BananaRoundLimitExceededError hard-stop at
# round 10 server-side) that task-1788202739's RFC covers. Landed after
# Ian's explicit go-ahead (#general 02:58 UTC) and three-way alignment
# in #agent-chat (Zero's technical claim + Amos's explicit ratification
# at 03:27 UTC, closing the RFC). Verified clean in an isolated /tmp
# venv before touching this file; note the library shipping `round`
# doesn't by itself wire handoff.py/topic_tracker.py to use it — that's
# separate follow-up work, not done by this bump.
#
# 2026-09-03: bumped v0.2.0 -> v0.5.1 (commit 0e802c5, PR #9 upstream —
# no v0.5.1 tag exists yet so pinned to the commit directly, same as the
# original 9298d7d pin before v0.1.1 was tagged). Fixes Amos's finding 4
# (should_reply() no longer turns a peer's reply:none yield into a
# summons) and finding 5 (default max_rounds 2 -> 4, was clamping
# legitimate multi-round dialogue early); adds an `sdk` wire field for
# channel-visible version tracking. Verified clean in an isolated /tmp
# venv (import + HandoffEnvelope field check) before touching this file.
# No call-site impact today: grepped bin/agent-server.py and
# bin/speaking_banana.py, neither references should_reply() or
# max_rounds from this package yet, so this is pure upside with no
# behavior change on my side. Requested by Ryan/Zero in #lounge, landed
# on "Moon Problem"/Ian's go-ahead there ("do it", 23:56 UTC). Same as
# the last two bumps: package upgrade only picks up on the next
# agent-server.py restart (Ian/Mike's call, not self-triggerable) since
# it's imported once at process start via speaking_banana.py.
banana-protocol @ git+https://github.com/brockventures/banana-protocol.git@0e802c5
